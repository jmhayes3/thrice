{
  "name": "thrice-backend",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev",
    "build": "npm run build:css",
    "build:css": "npx tailwindcss -i ./src/styles.css -o ./public/static/styles.css",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
