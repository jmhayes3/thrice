{
  "name": "thrice",
  "version": "0.0.1",
  "description": "Thrice emulator.",
  "license": "MIT",
  "scripts": {
    "test": "echo \"This is a test!\"",
    "build": "npm run build:css",
    "build:css": "npx tailwindcss -i ./backend/src/styles.css -o ./backend/public/styles.css",
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public"
  },
  "dependencies": {}
}
